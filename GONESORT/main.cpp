#include <bits/stdc++.h>
#define in(n) scanf("%d",&n)
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        in(n);
        int a[n];
        for(int i=0;i<n;i++)
            in(a[i]);
        int b[n];
        for(int i=0;i<n;i++)b[i]=a[i];
        sort(b,b+n);
        for(int i=0;i<n;i++)
        {

        }
    }
    return 0;
}
