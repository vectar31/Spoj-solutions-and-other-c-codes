#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        vector < vector <int> > v(n+1);
        int matches=n*(n-1)/2;
        for(int i=0;i<matches;i++)
        {
            int a,b;
            cin>>a>>b;
            v(a).push_back()
        }
    }
    return 0;
}
